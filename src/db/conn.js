// const mongoose = require("mongoose");

// mongoose.connect(
//     process.env.MONGODB_URI || 'mongodb://localhost:27017/FormSubmission').then(() => {
//     console.log(`connection successful`);
// }).catch((e) => {
//     console.log(`No Connection`);
// });